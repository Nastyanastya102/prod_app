import React, { memo } from 'react';
import { useTranslation } from 'react-i18next';

const ArticleDatailsPage = () => {
    const { t } = useTranslation('article');

    return (
        <div>
            
        </div>
    );
};

export default memo(ArticleDatailsPage);
